
[project]
name = "guidedresearch"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"

requires-python = ">=3.12,<3.13"
dependencies = [
    "datasets>=5.0.0",
    "kernelbench",
    "torch",
    "vllm==0.25.1",

]

[dependency-groups]
dev = ["pytest>=8"]

[tool.uv.sources]
kernelbench = { git = "https://github.com/ScalingIntelligence/KernelBench.git" }

[tool.pytest.ini_options]
testpaths = ["triton_lint/tests"]

