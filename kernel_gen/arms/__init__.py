"""Experiment arms. One module per arm, each a thin ``main()`` over ``kernel_gen.core``.

Run from the repo root: ``uv run python -m kernel_gen.arms.lintloop --level 1 --all``.
"""

from __future__ import annotations
